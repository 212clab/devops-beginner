# mongodb-pvc.yaml (P. 7의 StatefulSet volumeClaimTemplates에서 15Gi를 참조할 예정이므로, 여기서는 임시 파일만 생성합니다.)
# 실제 Mongodb PVC는 StatefulSet의 VolumeClaimTemplates에 의해 동적으로 생성되므로, 해당 파일을 일단 비워두고 다음 단계로 넘어갑니다.
# (파일 구조 유지)